/**
 * Represents a User Entity with contents of a user.
 */
export class UserEntity {
    firstName: string;
    lastName: string;
    email: string;
    username: string;

    // Array containing image information
    images: Array<string>;
}
