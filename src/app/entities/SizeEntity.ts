/**
 * Represents a Size Entity of a Size..
 * Contains all URL sizes of an image.
 * to create image URL.
 */
export class SizeEntity {
    label: string;
    width: number;
    height: number;
    source: string;
    url: string;
    media: string;
}
