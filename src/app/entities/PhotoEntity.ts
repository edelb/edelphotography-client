/**
 * Represents a Photo Entity with contents of an image
 * to create image URL.
 */
export class PhotoEntity {
    id: string;
    secret: string;
    server: string;
    farm: number;
    title: string;
}
